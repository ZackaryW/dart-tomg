name: dart_tomg_ci_test
description: Repository-only black-box verification for dart-tomg.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  path: ^1.9.1
  test: ^1.25.6
  yaml: ^3.1.3
